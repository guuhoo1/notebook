# 编辑面板优化方案

## 1. 问题分析

### 1.1 当前问题

#### 宽度限制问题
- **NoteEditor.vue** 中使用 `max-w-4xl` 限制了最大宽度，在大屏幕上编辑空间受限
- **MarkdownEditor.vue** 中预览面板固定为 `w-1/2`，无法根据内容灵活调整
- 缺少预设宽度方案，用户无法快速切换不同布局
- 响应式布局仅支持 md 断点以上显示预览

#### 交互流畅度问题
- 内容更新时缺少防抖处理，可能导致频繁重渲染
- 编辑器缺少虚拟滚动，长文档性能下降
- 缺少平滑的动画过渡效果
- 面板展开/收起无动画

### 1.2 用户使用场景
- **日常笔记写作**：需要舒适的阅读宽度（约 65-75 字符/行）
- **代码/文档编辑**：需要更宽的编辑空间以显示完整内容
- **Markdown 写作**：需要同时查看编辑和预览，双栏布局
- **移动设备编辑**：需要优化的移动端体验

---

## 2. 竞品分析

### 2.1 Notion
- **宽度**：默认居中，最大宽度 ~900px，支持全屏模式
- **布局**：可折叠侧边栏，内容区域自适应
- **交互**：流畅的拖拽和动画
- **特点**：聚焦模式隐藏所有干扰元素

### 2.2 Obsidian
- **宽度**：可配置的阅读宽度，编辑区域可拖拽调整
- **布局**：多面板布局，支持左右拆分
- **交互**：快速响应，支持 Vim 模式
- **特点**：高度可定制

### 2.3 语雀
- **宽度**：自适应宽度，支持全屏编辑
- **布局**：清晰的工具栏，实时预览
- **交互**：流畅的富文本编辑体验
- **特点**：协作编辑，版本历史

---

## 3. 优化方案设计

### 3.1 面板宽度优化

#### 功能特性
1. **可拖拽调整**：编辑区和预览区之间可拖拽调整比例
2. **预设宽度方案**：
   - 窄版（500px）：适合专注写作
   - 标准版（700px）：舒适阅读宽度
   - 宽版（900px）：适合长内容编辑
   - 全屏：占满可用空间
3. **双栏/单栏切换**：支持单栏编辑或双栏编辑+预览
4. **响应式布局**：
   - < 768px：单栏模式
   - 768-1024px：默认双栏，可切换
   - > 1024px：双栏默认，支持拖拽调整

#### 布局结构
```
┌─────────────────────────────────────────────────┐
│  ← 返回    [标题输入]    [存为草稿] [保存]   │
├─────────────────────────────────────────────────┤
│ [窄版] [标准版] [宽版] [全屏] [单/双栏] │
├──────────────────┬──────────────────────────────┤
│                  │                              │
│   编辑区域       │      预览区域                │
│                  │                              │
│              [拖拽手柄]                          │
│                  │                              │
└──────────────────┴──────────────────────────────┘
```

### 3.2 交互流畅度优化

#### 性能优化
1. **防抖处理**：内容更新防抖 300ms
2. **虚拟滚动**：长文档使用虚拟列表
3. **懒更新**：预览区延迟更新
4. **减少重渲染**：合理使用 memo

#### 动画效果
1. **面板切换动画**：平滑的展开/收起过渡
2. **宽度调整动画**：拖拽时的平滑反馈
3. **模态框动画**：优雅的出现/消失
4. **微交互**：按钮悬停、点击反馈

---

## 4. 技术实现方案

### 4.1 核心组件

#### ResizablePanel 组件
- 功能：可拖拽调整宽度的面板
- 实现：使用 VueUse 的 useResizeObserver
- 属性：minWidth, maxWidth, defaultWidth

#### WidthPreset 组件
- 功能：预设宽度选择器
- 选项：窄版、标准版、宽版、全屏

#### MarkdownEditor 优化
- 双栏可拖拽布局
- 支持单/双栏切换
- 预览区防抖更新

### 4.2 性能优化策略

#### 资源加载
- 按需加载编辑器扩展
- 图片懒加载

#### DOM 操作优化
- 使用虚拟滚动处理长列表
- 减少不必要的 DOM 更新
- 使用 CSS transform 替代 top/left 动画

#### 状态管理
- 使用 Pinia 管理编辑器状态
- 合理使用 computed 和 watch
- 避免深度监听

### 4.3 浏览器兼容性
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

## 5. 开发优先级与里程碑

### 5.1 优先级划分

#### P0（必须）
- 移除 max-w-4xl 限制
- 实现可拖拽调整宽度
- 实现预设宽度方案
- 基础响应式布局
- 防抖处理

#### P1（重要）
- 虚拟滚动
- 平滑动画过渡
- 性能监控
- 完整测试

#### P2（可选）
- 主题切换
- 快捷键支持
- 更多预设方案

### 5.2 里程碑

| 阶段 | 交付物 | 时间 |
|------|--------|------|
| 1 | 宽度优化完成 | 1天 |
| 2 | 交互优化完成 | 1天 |
| 3 | 测试与修复 | 0.5天 |
| 4 | 文档整理 | 0.5天 |

---

## 6. 工作量预估

| 任务 | 工作量 |
|------|--------|
| 分析与方案设计 | 2h |
| 宽度优化实现 | 4h |
| 交互优化实现 | 4h |
| 性能优化 | 3h |
| 测试与调试 | 3h |
| 文档编写 | 2h |
| **总计** | **18h** |

---

## 7. 技术风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 拖拽性能问题 | 中 | 中 | 使用 requestAnimationFrame 优化 |
| 兼容性问题 | 中 | 低 | 提供降级方案，使用 feature detection |
| 性能不达标 | 高 | 中 | 提前进行性能测试，预留优化时间 |
| 测试覆盖不足 | 中 | 低 | 制定详细测试用例，保证覆盖 |

---

## 8. 验收标准

### 8.1 功能验收
- [ ] 支持拖拽调整编辑区和预览区宽度
- [ ] 支持 4 种预设宽度方案
- [ ] 支持单栏/双栏切换
- [ ] 响应式布局在各断点正常工作
- [ ] 关键交互响应时间 < 100ms

### 8.2 性能验收
- [ ] Lighthouse 性能评分 > 90
- [ ] 长文档（>1000行）滚动流畅
- [ ] 内存占用无明显泄漏
- [ ] 无明显的掉帧现象

### 8.3 兼容性验收
- [ ] 在目标浏览器中功能正常
- [ ] 无样式错乱
- [ ] 交互流畅
